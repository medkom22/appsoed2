import 'package:flutter/material.dart';

class ExampleApp extends StatelessWidget {
  const ExampleApp({Key? key}) : super(key: key);
//menambahkan komen pada app
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
